name: Release

on:
  push:
    branches:
      - release/**

permissions:
  contents: write

jobs:
  central_release:
    name: Publish to Maven Central
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # required for vercraft to calculate version from git history
          submodules: true

      - uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - uses: gradle/actions/setup-gradle@v4

      - name: Get version from vercraft
        id: version
        run: |
          VERSION=$(./gradlew -q properties --console=plain 2>/dev/null | grep "^version:" | awk '{print $2}')
          echo "version=v${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Git status before
        run: git status

      - name: Publish to Maven Central
        run: ./gradlew publishAllPublicationsToMavenCentralRepository --no-configuration-cache --console=rich
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SONATYPE_USER }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SONATYPE_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.PGP_SEC }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.PGP_PASSWORD }}

      - name: Git status after
        run: git status

  github_release:
    name: Create GitHub Release
    needs: central_release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.central_release.outputs.version }}
          name: ${{ needs.central_release.outputs.version }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
